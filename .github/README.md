# .github

GitHub repository configuration and automation for fiducia-backend.

- **`dependabot.yml`** — weekly Dependabot update PRs for Cargo dependencies and
  GitHub Actions.
- **`workflows/`** — the CI and deploy pipelines (see that folder's README).
